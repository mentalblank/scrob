---
// Only allow POST to prevent logout CSRF.
if (Astro.request.method !== "POST") {
  return Astro.redirect("/login");
}
Astro.cookies.delete("token", { path: "/" });
return Astro.redirect("/login");
---
